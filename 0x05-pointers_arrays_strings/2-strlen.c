#include "main.h"

/**
 * _strlen - returns the length of a string
 * @*s: string
 * Return: The length of a string;
 */

int main(void)
{
	char *s;
	int len;

	str = *s;
	length = _strlen(str);
	printf("%d\n", len);
	return (len);
}
